# Render Static Site Configuration for StockQueen
# Note: Render reads render.yaml from the repo root, not this file.
# This file is kept for reference only.
# No routes needed - Render serves static files directly from staticPublishPath.
