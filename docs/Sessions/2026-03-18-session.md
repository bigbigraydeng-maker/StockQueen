---
name: Claude 工作记录 2026-03-18
date: 2026-03-18
type: session-log
tags: [session, strategy-matrix, backtest, bugfix]
---

# Claude 工作记录 — 2026-03-18

← [[Sessions/2026-03-13-session|← 13]] | [[Sessions/2026-03-19-session|19 →]]

---

## 完成工作

### 1. 策略矩阵整合进 /backtest 页面

- 多策略回测引擎（V4 + MR + ED 三策略对比）
- 资金分配方案对比（保守/均衡/进攻）
- 历史回测 JSON 数据文件（2018-2024）
- commit: `157133e`→`0fffb46`

### 2. 轮动历史数据修复

- `rotation-history` API 去重 + 静态 JSON 补全
- 更新静态数据到 2026-03-17
- 新增 `hold_days`（持有天数）字段
- 移除无意义的 Hold Days 列
- commit: `1b7289f`→`4e6bb7d`→`27d5232`→`bd4822e`

### 3. Quotes 页面修复

- `quotes-table` 500 错误：`is_held=None` 导致排序 `unary -` 崩溃
- commit: `0089145`

### 4. DB 导入修复

- 修复 `get_db` 未导入导致 6 个端点返回空（rotation-history 等）
- commit: `75bc868`

### 5. 年度业绩降级处理

- `yearly-performance` 端点 DB 数据不足时降级到静态 JSON
- commit: `a561b07`

### 6. Scan Cache DB 去重

- 防止 DB 查询重复：加 `_scan_cache_db_checked` 标记
- commit: `69233e4`

### 7. 官网选股池数字修正

- 首页 195→500（与 V5 实际配置一致）
- commit: `15dbcdf`

---

## 背景

3-18 是大量 Bug 消除日。V5 进入 Render 生产后发现多个因为环境差异导致的问题（DB 导入路径、缓存失效等）。

---

## 技术备注

策略矩阵 `portfolio_manager.py` 在此时已发现数据不一致问题（V4 每次跑出不同结果），这个 Bug 在 3-19 才正式修复。
