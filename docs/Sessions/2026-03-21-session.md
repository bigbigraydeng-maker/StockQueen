---
name: Claude 工作记录 2026-03-21（性能攻坚 + C4内幕信号 + 社交引擎）
date: 2026-03-21
type: session-log
tags: [session, performance, precompute, edgar, social, trailing-stop, equity-curve]
---

# 🔥 Claude 工作记录 — 2026-03-21 超级工作日

← [[Sessions/2026-03-20-session|← 20]] | [[Sessions/2026-03-22-session|22 →]]

---

## 完成工作

### 1. Cloudflare 524 超时根治 ✅

回测页面频繁 524 错误，根因是同步 Supabase 调用阻塞事件循环。

- Supabase cache 查询加 8s 超时
- 回滚 `ClientOptions` import（supabase 2.3.0 不支持）
- 全局 socket 超时 15s，防止同步调用永久阻塞
- 启动时 Supabase 调用改用 `asyncio.to_thread` + 10s 超时
- AV API 请求频率 0.8s→1.5s（75→40次/分）
- commit: `5264c1b`→`053a330`→`db482b0`→`8f78a0c`→`185704d`→`39337c2`→`f63589a`

### 2. 回测预计算迁移 GitHub Actions ✅

预计算在 Render 上跑 21h，严重拖慢服务。彻底迁移至 CI。

- 回测主循环每20周让出事件循环
- 每个 combo 放进 `asyncio.to_thread`
- 迁移至 GitHub Actions，彻底脱离 Render
- ticker 过滤 + 并行化，**从 21h 降至 ~2h**
- 添加手动触发接口 + admin precompute 端点改 GET
- 启动时预热 50 个回测 combo 进 L1 内存缓存
- commit: `9562d68`→`8b67ebe`→`3df1e5b`→`13ac16f`→`3c4ac9f`→`e8b1fcf`→`d5c354e`→`1f5e2f7`→`461c315`

### 3. 因子权重 IC 验证 + Scorer 升级 ✅

- IC 验证后自动更新因子权重
- `momentum_weights` 参数化
- commit: `dbce88c`

### 4. 各种 Bug 修复 ✅

- scheduler: `misfire_grace_time=3600` + 全量 `exc_info=True`
- web: yearly-performance DatetimeIndex bool + Windows UTF-8 encoding
- newsletter: `social_generator` 变量未初始化
- backtest: `end_date` 统一改为动态最近周五，解决缓存 key 不匹配
- config: `luohan.json` 多余括号导致策略页不加载
- strategy: null 字段乘法导致策略页 500 错误
- admin: 统一 admin token 环境变量名为 `ADMIN_API_KEY`
- commit: `d85125a`→`2f35eb9`→`c6d9b8b`→`94e685f`→`07d21f6`→`f92503c`→`d1d7343`

### 5. SEC Form 4 内幕交易信号系统 (C4) ✅

- 接入 SEC EDGAR Form 4 数据源
- 解析内幕交易买入/卖出信号
- 集成到信号面板
- commit: `0bf7897`→`14ff321`

### 6. 品牌资产升级 ✅

- 正式化 StockQueen logo，全站更新品牌资产
- commit: `cd8deee`

### 7. ED 事件驱动 Regime 门控 ✅

- bull/strong_bull 时停止 ED 入场（牛市不做事件驱动）
- 事件信号面板上线，移除飞书推送
- commit: `2390ba7`→`c0a7ae9`→`10cd244`→`6c483c5`

### 8. 止盈止损根治 ✅

Tiger 模拟盘价格延迟导致止盈/止损不触发。

- 修复 cache_store `updated_at` 不更新
- 新增手动触发接口
- 彻底根治价格来源问题
- commit: `520b08c`→`3b3fa10`→`179ebe3`→`3fe3265`

### 9. AI 情绪扩展 + Sector Wind ✅

- AI 情绪覆盖扩展至动态池 Top 100
- ETF flow 接入 `sector_wind`
- commit: `8e25d89`

### 10. 回测增强 ✅

- 新增 `pure_alpha_comparison.py` + `disable_regime_filter` 参数
- FMP 升级至高级版，4个基本面 collector 覆盖完整动态池
- 默认起始日期 2022→2018，扩展预取数据范围
- Regime State Machine 静态 fallback
- 方法论说明仓位数动态跟随 `top_n`
- commit: `27256d3`→`71b9a1d`→`c594688`→`fe42268`→`2c142fb`

### 11. Equity Curve 图表 ✅

反复调试 Chart.js 渲染问题，最终采用对数坐标轴。

- 修复 Y轴范围异常（-2000~2000）
- 强制等待 browser reflow 后再渲染
- 对数坐标轴 + 固定高度包装，SPY/QQQ 全程可见
- 去掉 Strategy `fill=true`（对数坐标下覆盖基准）
- 最终删除 Equity Curve 模块（决定不保留）
- commit: `c4d8c6d`→`3b94405`→`6e0babb`→`46631f7`→`3b8da94`→`36a13ea`→`2ae5181`→`1da587c`

### 12. 社交图片生成引擎 ✅

- 多维度图片生成（5张卡片全面重设计）
- AI 文案引擎升级（DeepSeek→OpenAI）
- 跨平台字体加载 + Render CJK 字体安装
- 字体下载脚本
- commit: `44b5687`→`3bd79d7`→`dfceb68`→`efe3bf3`→`c4f374b`→`202dd47`→`1470c58`→`3901615`→`e6b6042`

### 13. 模拟盘对比页面修复 ✅

- 修复 `'d is not defined'` 崩溃
- commit: `ae3ba2a`

---

## 关键成果

| 指标 | 改善 |
|------|------|
| 回测预计算时间 | 21h → ~2h（10倍提速）|
| Cloudflare 524 | 根治（全链路异步化）|
| 止盈止损触发 | 根治（价格来源统一）|
| 新系统 | C4 内幕交易信号 |
| 社交运营 | 5张卡片 + AI文案 |

---

## Git 记录

**主要 merge:**
- `1748dc9` merge: worktree fixes + weekly data refresh
- `206b96a` / `9a18a00` Merge claude/quizzical-hopper
- `14ff321` merge: SEC Form 4 内幕交易信号系统 (C4)
- `156e328` Merge claude/relaxed-volhard
- `f2544a0` Merge claude/eloquent-euler

**当日 commit 数**: 50+（史上最多单日提交）

---

## 下一步

- [ ] 数据源迁移 AV+FMP → Massive
- [ ] SEC EDGAR 内幕信号博客
- [ ] Newsletter 审批流程完善
